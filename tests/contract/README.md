# Contract Tests

Use this directory for provider-native contract tests and golden normalization snapshots.

Contract tests should prove that authoritative provider payloads match the provider-native protobuf contracts and normalize into the expected canonical outputs.
